// Other
import * as ping from "./other/ping";

// Gamble
import * as gamble from "./gamble/gamble";
import * as cookies from "./gamble/cookies";
import * as leaderboard from "./gamble/leaderboard";

export const commands = {
  ping,
  gamble,
  cookies,
  leaderboard,
};
